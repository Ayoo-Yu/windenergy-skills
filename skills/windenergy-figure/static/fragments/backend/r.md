# R Backend

Use ggplot2, patchwork, ComplexHeatmap, or base R as appropriate. Do all
drawing, previewing, exporting, and QA in R once this backend is selected. If
R/Rscript or required packages are unavailable, report the blocker and do not
silently switch to Python.

