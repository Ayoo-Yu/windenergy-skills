# Python Backend

Use Matplotlib, Seaborn, NumPy, pandas, or SciPy when available. Do all drawing,
previewing, exporting, and QA in Python once this backend is selected. If a
required Python package is unavailable, report the blocker and do not silently
switch to R.

